var express = require('express');
var router = express.Router();


const cartController = require('../controllers/cartController');


/* router.post('/',cartController.store); */
/* router.delete('/:id', cartController.destroy);
router.get('/:id/productos',cartController.index);
router.post('/:id/productos',cartController.update);
router.delete('/:id/productos/:id_prod',cartController.delete); */




module.exports = router;